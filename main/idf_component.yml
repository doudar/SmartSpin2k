## IDF Component Manager Manifest File
dependencies:
  espressif/arduino-esp32: "^3.1.0"
  # Note: External Arduino libraries (esp-nimble-cpp, TMCStepper, ArduinoJson, 
  # FastAccelStepper, ArduinoWebsockets) will be downloaded and managed by 
  # the CMakeLists.txt using FetchContent during the build process
